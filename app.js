// create selectors for input and button 

/* write a function countUniqueValues that accepts a sorted array, and counts the unique values in the array. There can be negative numbers in the array, but it always will be sorted.
Examples: 
countUniqueValues([1, 1, 1, 2]) // 2
countUniqueValues([]) // 0
countUniqueValues([-2, -1, -1, 0, 1]) // 4 */

const inputByClassName = document.getElementsByClassName('todo-input')
console.log('inputByClassName: ', inputByClassName)

const buttonByClass = document.querySelector('button.todo-button')
console.log('BUTTON by CLASS name: ', buttonByClass)

